import { View } from "react-native";

/**
 * Mock implementation for WebView component.
 */
export const WebView = (props) => {
  // You can render a simple View or a more detailed mock
  // depending on what you need to test.
  return <View testID="mock-webview" {...props} />;
};
